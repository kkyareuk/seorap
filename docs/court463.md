# 궁정 이야기 첫 구현 — 1.0.411 (463)

2026-09-20. origin/dev 00e747a2에서 분리한 개발 작업. 게임 main에는 병합하지 않는다.

## 사용 방법

광장 → 함께하는 활동 → 궁정 이야기 · 그룹 배경 → 멀티 그룹 선택.
방장/관리자가 로판 궁정 배경을 저장한다. 각 소유자는 자기 캐릭터의 신분, 정치적 입장, 대화 성향, 공개 소개를 저장하고 자동 대화를 허용한다.
같은 마을에서 자동 대화를 허용한 다른 캐릭터를 선택한 뒤 장면을 고른다.

## 포함 범위

- 궁정 인사, 연회 후 휴식, 차 취향, 작은 부탁, 의견 차이: 5개 장면, 각 3개 선택지.
- 상대의 성향과 관계에 따라 반응과 친밀도·애정도·신뢰·편안함·갈등도 변화. 상대가 나에게 느끼는 방향으로 저장한다.
- 사회적 거리는 별도 설정값. 낮을수록 대외적으로 친하게 지내기 쉽고, 장소의 격식과는 별개다. 기본 설정에서 계산하며 관리자가 두 사람의 값을 0–100으로 직접 고정할 수 있다. 일반 대화는 값을 바꾸지 않는다.
- 타인의 캐릭터 설정 변경 금지, 자동 대화 동의, 차단 관계 확인, 소유권·그룹·마을 확인, 만료 및 설정 변경 감지.
- 서버 판정, 선택 중복 요청의 동일 결과 반환, 두 캐릭터의 1시간 내 추가 수치 변화 제한, 관계 고정 설정 준수.
- 대화 기록은 시작한 계정만 API로 조회한다. 소개는 공개 역할극 설정이며 자연어 자동 판정이나 비밀 저장소가 아니다.
- 새 UI와 콘텐츠의 영어·일본어 번역 포함. 서버 콘텐츠 49개 다국어 항목 검사. 전체 기존 앱 번역률은 재측정하지 않음.

현재는 광장 내 궁정 대화에 적용하는 첫 버전이다. 기존 모든 일상 로그의 궁정화, 의상 대여, 자동 연애 확정, 추리극, 싱글 NPC 스토리는 포함하지 않는다.

## 검증

- `node scripts/check-court463.cjs`: 권한·동의·차단·재시도·중복 선택·만료·설정 변경·쿨다운·고정 모드·방향별 관계·사회적 거리 324조합 통과.
- `node scripts/qa-court463.mjs` 및 `--webkit`: 격리된 테스트 DB에서 실제 서비스 모듈과 UI를 연결, 방장/멤버/상대 계정 구분, 동의 취소, 선택 결과, 개인 기록, 광장 진입/복귀, KO/EN/JA 모바일·태블릿 검사 통과. 운영 두 계정 실기기 검증은 아님.
- `node scripts/check-shared431.mjs`: 기존 멀티 활동/동행/개인 상태 보존 통과.
- 웹 준비 과정에서 누락된 기존 home-canvas.css 포함 목록을 보완하여 웹 모듈 준비 통과.
- iOS 프로젝트/공통 자산 준비 검사 통과. Windows에서 CocoaPods·Xcode·IPA 서명·실기기 검증·TestFlight 업로드는 실행하지 않음.
- 기존 `check-shared-town-service.cjs` 60행과 `check-relationship-emotion-202.mjs` 80행 실패는 수정 전 00e747a2에서도 동일하게 재현. 이번 변경으로 생긴 실패가 아님.

## 배포 상태

- 구현 커밋 `2e9f5d1f`, 소유자 변경 시 이전 동의를 재사용하지 않는 서버 보강 `36d2f70f`를 origin/dev에 반영했다.
- `sharedTownApi`와 Firestore 규칙 배포 완료. 인증 없는 궁정 API 요청은 HTTP 401로 거절됨을 확인했다. 기존 그룹의 세계관이나 참가 동의는 자동 변경하지 않았다.
- Android 최종 AAB 서명 확인 및 준비된 웹 자산 494개 바이트 일치 검사 통과.
- 파일: `C:/Users/Public/drawer-releases/drawervillage-1.0.411-463-internal.aab`
- SHA256: `14D8D9C0AA261BFD098DF67F96626E505376B017F2A158D681070CE50C168ECD`
- Play Console에서 기존 최신 프로덕션/내부 462를 확인하고, 내부 release342에 463 및 KO/EN/JA 출시노트를 업로드했다. **2026-09-20 15:45 KST, ‘내부 테스터에게 제공됨’ 확인 완료.** 프로덕션 승격 없음.
- 운영 웹/main/프로덕션 승격과 공지 발송은 이번 작업에 포함하지 않는다.
